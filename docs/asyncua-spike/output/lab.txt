[supported] browse.continuation: Large folder: 1200 references over 12 Browse/BrowseNext pages (server cap 100 per node); release -> Good
[supported] read.operation-limits: MaxNodesPerRead=40; a Read of 50 raises BadTooManyOperations (code 0x80100000): the ServiceFault arrives as a typed UaStatusCodeError, which operation_limits.py chunking avoids
[supported] history.raw-continuation: Dynamic/Ramp over 40s, NumValuesPerNode=0: 44 values over 2 pages (server pages at 25)
[differs] values.structure-live: ["Range: VariantType=ExtensionObject, Python type Range, records.variant_to_json -> 'Range(Low=-50.0, High=250.0)'", 'EUInformation: VariantType=ExtensionObject, Python type EUInformation, records.variant_to_json -> "EUInformation(NamespaceUri=\'http://www.opcfoundation.org/UA/units/un/cefact\', UnitId=4408652, DisplayName=LocalizedText(Locale=\'en\', Text=\'°C\'), Description=LocalizedText(Locale=\'en\', Text=\'degree Celsius\'))"']. asyncua decodes namespace-0 structures into typed dataclasses, so the data is there for a Part 6 JSON encoding (#171); records.py still falls back to str()
[supported] namespaces.reconnect: lab namespace index 2 before the restart, 3 after --pad-namespace. The old client, with auto_reconnect=False, is DISCONNECTED and a call raises ConnectionError: client is disconnected; a fresh client's NamespaceArray is read from the server, never cached by asyncua, so the index moves (a stale ns=2 read -> BadNodeIdUnknown; ns=3 -> -42). The per-connection rebind in connection.py carries over
[differs] keepalive.stall-default: server SIGSTOPped for 2.5s, default watchdog_intervall=1.0: state DISCONNECTED, a read raises ConnectionError, connection_lost_callback ['TimeoutError at 2.0s']. asyncua's supervisor reads ServerState every watchdog_intervall with that as its timeout, and with auto_reconnect=False a single missed probe ends the session for good; python-opcua has no such probe (its KeepAlive thread only renews the channel), so today a 2.5s stall costs nothing
[differs] keepalive.stale-subscription: server SIGSTOPped for 9s with watchdog_intervall=30 (probe out of the way) and one subscription (200ms x 20 keep-alives): state CONNECTED, a read works, connection_lost_callback never; subscription id 1 -> 2. The stale-subscription watchdog runs even with auto_reconnect=False and re-creates the subscription under a NEW id without telling the caller
